{
  "name": "@alethia/shared",
  "version": "0.1.0",
  "private": true,
  "types": "./types/index.ts",
  "exports": {
    ".": "./types/index.ts",
    "./types/*": "./types/*.ts"
  },
  "scripts": {
    "build": "echo 'shared: no build needed (types only)'"
  }
}
